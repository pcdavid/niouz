Lots:
 * fix tests,  more tests
 * document
 * implement authorization logic
 * config file support
 * authorization should be configurable
 * implement command messages (cancelation,...)
 * switch to eventmachine
 * message bus if sth happened (esp. a article was posted/canceled)
 * active record backend as separate gem
